const Discord = module.require("discord.js");
const fs = require("fs");
let config = require("../desc.json");

module.exports.run = async (client,message,args) => {
	let user = message.guild.member
	}


	module.exports.help = {
    name: "desc"
};